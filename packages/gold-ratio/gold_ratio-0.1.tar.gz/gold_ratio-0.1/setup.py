# setup.py

from setuptools import setup, find_packages

setup(
    name='gold_ratio',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Tidak ada dependensi eksternal
    ],
)
