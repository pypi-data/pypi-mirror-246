[package]
name = "aegispy"
version = "0.1.1"
edition = "2021"

[lib]
name = "aegis"

[dependencies]
pyo3 = "0.20"
zeroize = { version = "1.7", features = [ "zeroize_derive" ] }
cfg-if = "1.0"
softaes = "0.1"

[dev-dependencies]
serde_json = "1.0"
hex = "0.4"
