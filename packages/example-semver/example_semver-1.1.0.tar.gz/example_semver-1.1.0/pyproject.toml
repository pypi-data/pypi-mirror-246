[tool.poetry]
name = "example-semver"
version = "1.1.0"
description = ""
authors = ["Muhammad Luqman <im.imperativa@gmail.com>"]
readme = "README.md"
packages = [{include = "example_semver"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
