[tool.poetry]
name = "vernissage"
version = "0.2.0"
description = "Photo editing, add border and resize."
authors = ["Rikles <dev@rikles.fr>"]
readme = "README.md"
repository = "https://codeberg.org/rikles/vernissage"

[tool.poetry.dependencies]
python = "^3.12"
pillow = "^10.1.0"
click = "^8.1.7"

[tool.poetry.scripts]
vernissage = "vernissage.cli:cli"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
