[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rubin.citsci"
version = "0.2.4"
readme = "README.md"
dependencies = [
    "panoptes_client",
    'google.cloud.storage',
    'requests',
]