[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "iris-jwst"
version = "0.2.8"
authors = [
  { name="Carlos E. Muñoz-Romero", email="carlos.munoz_romero@cfa.harvard.edu" },
]
description = "iris-jwst is a GPU-accelerated package to generate high-resolution opacity-weighted molecular line intensities to model JWST MIRI MRS spectra."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[tool.poetry.dependencies]
astroquery = "^0.4.6"
jax = "^0.4.21"
jaxlib = "^0.4.19+cuda11.cudnn86"


[project.urls]
Homepage = "https://github.com/munozcar/IRIS"
