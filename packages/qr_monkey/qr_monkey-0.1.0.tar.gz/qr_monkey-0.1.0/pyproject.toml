[tool.poetry]
name = "qr_monkey"
version = "0.1.0"
description = "An API Client for qrcode-monkey API that integrates custom and unique looking QR codes into your system or workflow."
authors = ["Vansh Murad Kalia"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
