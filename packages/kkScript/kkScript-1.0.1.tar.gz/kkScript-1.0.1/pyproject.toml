[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kkScript"
version = "1.0.1"
description = "Simple database with own programming languge."
readme = "README.md"
authors = [{ name = "EasyOmny", email = "omenplay325@gmail.com" }]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = ["python", "database", "language"]

requires-python = ">=3.9"

[project.urls]
Homepage = "https://github.com/EasyOmny/kkscript"
