[build-system]
requires = [
"setuptools>=42",
"setuptools-scm",
"wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["SimuBox"]
include = ["*"]  # package names should match these glob patterns (["*"] by default)

[project]
name = "SimuBox"
version = "0.1.14"
authors = [
    { name = "Alkaid Yuan", email = "kryuan@qq.com" }
]
description = "Free python package to do some science calculation."
readme = "./SimuBox/README.md"
requires-python = ">3.9.7"
license = { file = "./SimuBox/LICENSE" }
classifiers =[
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "matplotlib",
    "scipy",
    "opencv-python",
    "numpy",
    "pandas",
    "pydantic",
    "tqdm",
    "shapely",
    "pyvista",
    "scikit-image",
    "scikit-learn",
    "sympy",
    "streamlit",
    "stpyvista",
    "pyecharts",
    "streamlit_echarts",
    "plotly"
]

[project.urls]
"Homepage" = "https://github.com/KangruiYuan/SimuBox"

[tool.setuptools_scm]
